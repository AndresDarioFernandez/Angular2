import { ShowOne } from "./show-one.directive";

describe('ShowOneDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOne();
    expect(directive).toBeTruthy();
  });
});
